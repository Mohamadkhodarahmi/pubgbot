# راهنمای استفاده از Telegram Bypass برای دور زدن فیلترینگ

## 📋 مقدمه

اگر سرور شما در ایران است و تلگرام فیلتر شده، می‌توانید از [TelegramByapss](https://github.com/MamadJS/TelegramByapss) برای دور زدن فیلترینگ استفاده کنید.

این راه‌حل از Cloudflare Workers استفاده می‌کند و درخواست‌های شما را از طریق یک ورکر به تلگرام ارسال می‌کند.

## 🚀 روش 1: استفاده از ورکر عمومی (ساده‌ترین روش)

### مراحل:

1. فایل `.env` را باز کنید
2. مقادیر زیر را تنظیم کنید:

```env
USE_BYPASS=true
TELEGRAM_BYPASS_URL=https://public-telegram-bypass.solyfarzane9040.workers.dev
```

3. ربات را مجدداً راه‌اندازی کنید

**نکته:** ورکر عمومی ممکن است محدودیت داشته باشد. برای استفاده بهتر، ورکر خودتان را بسازید.

## 🔧 روش 2: ساخت ورکر اختصاصی (توصیه می‌شود)

### مراحل:

1. **ایجاد اکانت Cloudflare**
   - به [cloudflare.com](https://cloudflare.com) بروید
   - یک اکانت رایگان بسازید

2. **ایجاد Worker**
   - وارد داشبورد Cloudflare شوید
   - از منوی سمت چپ، **Workers & Pages** را انتخاب کنید
   - روی **Create Application** کلیک کنید
   - روی **Create Worker** کلیک کنید
   - یک نام برای ورکر انتخاب کنید (مثلاً: `telegram-bypass`)
   - روی **Deploy** کلیک کنید

3. **کپی کردن کد**
   - به [مخزن GitHub](https://github.com/MamadJS/TelegramByapss) بروید
   - فایل `index.js` را باز کنید
   - محتویات آن را کپی کنید

4. **قرار دادن کد در Worker**
   - در داشبورد Cloudflare، ورکر خود را باز کنید
   - به بخش **Code** بروید
   - کد کپی شده را جایگزین کد موجود کنید
   - روی **Save and Deploy** کلیک کنید

5. **دریافت آدرس ورکر**
   - بعد از deploy، آدرس ورکر شما نمایش داده می‌شود
   - فرمت آن چیزی شبیه این است: `https://telegram-bypass.your-username.workers.dev`

6. **تنظیم در پروژه**
   - فایل `.env` را باز کنید
   - مقادیر زیر را تنظیم کنید:

```env
USE_BYPASS=true
TELEGRAM_BYPASS_URL=https://telegram-bypass.your-username.workers.dev
```

7. **راه‌اندازی مجدد ربات**

```bash
python bot.py
```

## ✅ تست کردن

بعد از راه‌اندازی، ربات باید به درستی کار کند. برای اطمینان:

1. ربات را در تلگرام پیدا کنید
2. دستور `/start` را ارسال کنید
3. اگر پیام خوش‌آمدگویی دریافت کردید، یعنی bypass کار می‌کند

## 🔍 عیب‌یابی

### مشکل: ربات کار نمی‌کند

- بررسی کنید که `USE_BYPASS=true` باشد
- بررسی کنید که `TELEGRAM_BYPASS_URL` درست باشد (بدون `/bot` در انتها)
- لاگ‌های ربات را بررسی کنید

### مشکل: خطای اتصال

- بررسی کنید که ورکر شما در Cloudflare فعال باشد
- بررسی کنید که آدرس ورکر درست باشد
- از ورکر عمومی استفاده کنید تا مطمئن شوید مشکل از کجاست

### مشکل: کندی ربات

- ورکر عمومی ممکن است کند باشد
- یک ورکر اختصاصی بسازید
- بررسی کنید که سرور شما به اینترنت دسترسی دارد

## 📝 نکات مهم

1. **امنیت:** ورکر عمومی را برای استفاده production توصیه نمی‌کنیم. بهتر است ورکر خودتان را بسازید.

2. **محدودیت‌ها:** Cloudflare Workers رایگان محدودیت دارد:
   - 100,000 درخواست در روز
   - 10ms CPU time per request

3. **هزینه:** برای استفاده بیشتر، می‌توانید پلن پولی Cloudflare را خریداری کنید.

4. **پشتیبان‌گیری:** اگر ورکر شما از کار بیفتد، می‌توانید `USE_BYPASS=false` کنید و از اتصال مستقیم استفاده کنید (اگر سرور شما خارج از ایران است).

## 🔗 لینک‌های مفید

- [مخزن GitHub TelegramByapss](https://github.com/MamadJS/TelegramByapss)
- [مستندات Cloudflare Workers](https://developers.cloudflare.com/workers/)
- [ورکر عمومی](https://public-telegram-bypass.solyfarzane9040.workers.dev/)

## 💡 پیشنهادات

- اگر سرور شما خارج از ایران است، نیازی به bypass ندارید
- برای استفاده production، حتماً ورکر اختصاصی بسازید
- لاگ‌های ورکر را در Cloudflare بررسی کنید تا از عملکرد آن مطمئن شوید

